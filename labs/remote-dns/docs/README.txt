This lab is not functional.  The publicly available bind programs do not let
us properly simulate a top level DNS.  They only act as a domain DNS.  It is
therefore challenging to return a poisoned .com DNS server.
Suggestions and worked examples of a functional lab would be welcome.
