service ssh
{
          socket_type            = stream
          protocol               = tcp
          wait                   = no
          port                   = RANDOM_PORT
          user                   = root
          server                 = /usr/sbin/sshd
          server_args            = -i
          disable                = no
}

