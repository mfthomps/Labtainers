service gps_tracker
{
    flags = REUSE
    socket_type = stream
    wait = no
    user = root
    server = /usr/sbin/GPS_Tracker
    log_on_failure += USERID
    log_on_success += USERID
    disable = no
}