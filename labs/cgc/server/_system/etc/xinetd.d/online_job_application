service online_job_application
{
    flags = REUSE
    socket_type = stream
    wait = no
    user = root
    server = /usr/sbin/online_job_application
    log_on_failure += USERID
    log_on_success += USERID
    disable = no
}