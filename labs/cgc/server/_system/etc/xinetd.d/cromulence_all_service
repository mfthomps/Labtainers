service cromulence_all_service
{
    flags = REUSE
    socket_type = stream
    wait = no
    user = root
    server = /usr/sbin/Cromulence_All_Service
    log_on_failure += USERID
    log_on_success += USERID
    disable = no
}