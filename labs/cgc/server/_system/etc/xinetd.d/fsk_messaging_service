service fsk_messaging_service
{
    flags = REUSE
    socket_type = stream
    wait = no
    user = root
    server = /usr/sbin/FSK_Messaging_Service
    log_on_failure += USERID
    log_on_success += USERID
    disable = no
}