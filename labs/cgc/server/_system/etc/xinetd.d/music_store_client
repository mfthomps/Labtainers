service music_store_client
{
    flags = REUSE
    socket_type = stream
    wait = no
    user = root
    server = /usr/sbin/Music_Store_Client
    log_on_failure += USERID
    log_on_success += USERID
    disable = no
}