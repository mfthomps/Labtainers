service filesystem_command_shell
{
    flags = REUSE
    socket_type = stream
    wait = no
    user = root
    server = /usr/sbin/Filesystem_Command_Shell
    log_on_failure += USERID
    log_on_success += USERID
    disable = no
}