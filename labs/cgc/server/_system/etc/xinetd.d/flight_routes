service flight_routes
{
    flags = REUSE
    socket_type = stream
    wait = no
    user = root
    server = /usr/sbin/Flight_Routes
    log_on_failure += USERID
    log_on_success += USERID
    disable = no
}