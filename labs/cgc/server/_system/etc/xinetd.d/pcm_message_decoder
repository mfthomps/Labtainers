service pcm_message_decoder
{
    flags = REUSE
    socket_type = stream
    wait = no
    user = root
    server = /usr/sbin/PCM_Message_decoder
    log_on_failure += USERID
    log_on_success += USERID
    disable = no
}