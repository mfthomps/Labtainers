service ecm_tcm_simulator
{
    flags = REUSE
    socket_type = stream
    wait = no
    user = root
    server = /usr/sbin/ECM_TCM_Simulator
    log_on_failure += USERID
    log_on_success += USERID
    disable = no
}