service virtual_machine
{
    flags = REUSE
    socket_type = stream
    wait = no
    user = root
    server = /usr/sbin/Virtual_Machine
    log_on_failure += USERID
    log_on_success += USERID
    disable = no
}