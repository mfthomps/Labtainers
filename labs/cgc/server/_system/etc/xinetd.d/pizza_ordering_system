service pizza_ordering_system
{
    flags = REUSE
    socket_type = stream
    wait = no
    user = root
    server = /usr/sbin/pizza_ordering_system
    log_on_failure += USERID
    log_on_success += USERID
    disable = no
}