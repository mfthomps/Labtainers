service snail_mail
{
    flags = REUSE
    socket_type = stream
    wait = no
    user = root
    server = /usr/sbin/Snail_Mail
    log_on_failure += USERID
    log_on_success += USERID
    disable = no
}