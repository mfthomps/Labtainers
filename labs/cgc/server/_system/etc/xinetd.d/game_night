service game_night
{
    flags = REUSE
    socket_type = stream
    wait = no
    user = root
    server = /usr/sbin/Game_Night
    log_on_failure += USERID
    log_on_success += USERID
    disable = no
}