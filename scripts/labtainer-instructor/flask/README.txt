README for Labtainers browser based assessment impelementation using Flask
server.py -- the flask server
static -- icons and the CSS
templates -- flask webpage templates


Use the -v option on the grader to share the development directory with the grader container, i.e.,
so that local changes appear in the running grader for quicker work flows.
